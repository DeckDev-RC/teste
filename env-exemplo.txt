# Copie este conteúdo para um arquivo .env na raiz do projeto

# ============================================
# CONFIGURAÇÕES DO SUPABASE (FRONTEND)
# ============================================
# URL do Supabase (self-hosted ou cloud)
# Para self-hosted: use HTTP (ex: http://31.97.164.208:8000)
# Para cloud: use HTTPS (ex: https://seu-projeto.supabase.co)
VITE_SUPABASE_URL=http://31.97.164.208:8000

# Chave anônima (anon key) do Supabase
# IMPORTANTE: Substitua pela chave do seu Supabase self-hosted ou cloud
# Obtenha na VPS com: docker exec supabase-kong env | grep ANON_KEY
VITE_SUPABASE_ANON_KEY=sua_chave_anon_aqui

# ============================================
# CONFIGURAÇÕES DO SUPABASE (BACKEND)
# ============================================
# URL do Supabase para o backend
SUPABASE_URL=http://31.97.164.208:8000

# Chave de serviço (service key) - Bypass RLS para operações do sistema
# IMPORTANTE: Mantenha esta chave segura! Nunca exponha no frontend!
# Obtenha na VPS com: docker exec supabase-kong env | grep SERVICE_ROLE_KEY
SUPABASE_SERVICE_KEY=sua_service_key_aqui

# Chave anônima para autenticação de usuários no backend
SUPABASE_ANON_KEY=sua_chave_anon_aqui

# ============================================
# CONFIGURAÇÕES DA API DO GOOGLE GEMINI
# ============================================
GEMINI_API_KEY=REMOVED_API_KEY

# ============================================
# CONFIGURAÇÕES DO AMBIENTE
# ============================================
NODE_ENV=development

# Instruções:
# 1. Crie um arquivo chamado .env na raiz do projeto
# 2. Copie o conteúdo acima para o arquivo .env
# 3. Substitua a chave da API pela sua chave real se necessário
# 4. Salve o arquivo

# IMPORTANTE: Nunca commite o arquivo .env em repositórios públicos!

# Notas sobre a API do Gemini:
# - Este projeto usa os modelos gemini-pro e gemini-pro-vision
# - Certifique-se de que sua chave API tem acesso aos modelos corretos
# - Para análise de texto: gemini-pro
# - Para análise de imagens: gemini-pro-vision

# IMPORTANTE: Nunca commite o arquivo .env em repositórios públicos!

# Notas sobre a API do Gemini:
# - A partir de 12 de julho de 2024, o modelo gemini-pro-vision foi substituído por gemini-1.5-pro-vision
# - Este projeto usa automaticamente o modelo mais recente (gemini-1.5-pro-vision)
# - Certifique-se de que sua chave API tem acesso aos modelos mais recentes 